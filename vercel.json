{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install --no-audit --no-fund",
  "framework": "nextjs",
  "regions": ["sin1"],
  "build": {
    "env": {
      "PUPPETEER_SKIP_DOWNLOAD": "true"
    }
  },
  "git": {
    "deploymentEnabled": {
      "main": false
    }
  },
  "rewrites": [
    {
      "source": "/ingest/static/:path*",
      "destination": "https://us-assets.i.posthog.com/static/:path*"
    },
    {
      "source": "/ingest/:path*",
      "destination": "https://us.i.posthog.com/:path*"
    }
  ],
  "headers": [
    {
      "source": "/u/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=300, s-maxage=3600"
        }
      ]
    }
  ],
  "crons": [
    {
      "path": "/api/admin/cutout-worker",
      "schedule": "* * * * *"
    }
  ],
  "functions": {
    "src/app/api/extraction/submit/route.ts": {
      "maxDuration": 120
    },
    "src/app/api/extraction/status/[jobId]/route.ts": {
      "maxDuration": 120
    },
    "src/app/api/admin/cutout-worker/route.ts": {
      "maxDuration": 120
    }
  }
}
# Studio control asset masters

Optional masters for:

- `rotate-left45`, `rotate-overhead`, `rotate-right45`
- `light-natural`, `light-moody`, `light-studio`

**Runtime loading uses `public/studio/controls/{name}.png`.** Copy or export PNG files there (same basenames + `.png`) so `/studio` can display them.
